<!DOCTYPE html>
<html lang="en">
{{> head}}

<body >

{{> topnav}}

<div id="content" class="container">
    <div class="row">
        <div class="col-md-3">
            <div id="toc" class="bs-sidebar affix bs-sidenav hidden-print"></div>
        </div>
        <div class="col-md-9 generatedMarkdown">
            <h1>{{page.title}}</h1>
            {{#markdown}}
                {{> body}}
            {{/markdown}}
        </div>
    </div>
</div>

{{> footer }}

{{> bottomIncludes }}

</body>
</html>
